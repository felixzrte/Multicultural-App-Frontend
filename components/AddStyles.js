import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  requiredText: {
    color: 'red',
  },
  headerText: {
    fontSize: 20,
    margin: 0,
    fontWeight: "bold"
  },
  menuContent: {
    color: "#000",
    fontWeight: "bold",
    padding: 2,
    width: '100%',
    fontSize: 20
  }
});